import React from 'react';
import ListingList from './ListingList'; // Make sure ListingList.tsx is in the same folder

const Listings: React.FC = () => {
  return <ListingList />;
};

export default Listings;
